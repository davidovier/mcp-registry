# E2E Test Configuration
# Copy this file to .env.test.local and fill in your values

# Supabase Configuration (same as your .env.local)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Service Role Key (for admin operations like creating test users)
# Get this from: Supabase Dashboard > Settings > API > service_role key
# WARNING: Never expose this key in client-side code
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# E2E Test User Credentials
# These credentials will be used to create/login a test user for authenticated tests
E2E_TEST_USER_EMAIL=e2e-test@example.com
E2E_TEST_USER_PASSWORD=a-secure-test-password-123

# Optional: Base URL for Playwright tests (defaults to http://localhost:3000)
# PLAYWRIGHT_BASE_URL=http://localhost:3000
