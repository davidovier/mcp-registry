# ===========================================
# MCP Registry - Environment Variables
# ===========================================
# Copy this file to .env.local and fill in the values
# NEVER commit .env.local to version control!

# ===========================================
# Supabase Configuration
# ===========================================
# Get these from your Supabase project dashboard:
# https://supabase.com/dashboard/project/_/settings/api

# Supabase project URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase anonymous (public) key - safe for client-side use
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# ===========================================
# Application Configuration (Optional)
# ===========================================

# App name displayed in the UI
NEXT_PUBLIC_APP_NAME=MCP Registry

# Site URL for auth redirects (magic links, OAuth callbacks)
# Set to your production URL in Vercel env vars
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ===========================================
# Server-only Variables (NOT exposed to browser)
# ===========================================
# These are only available in server-side code (API routes, server components)

# Supabase service role key - NEVER expose to client!
# Only use this for admin operations in secure server-side code
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
